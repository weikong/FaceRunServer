<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.facerun.dao.CustCircleMapper">

    <select id="getCircleList" parameterType="java.util.Map" resultType="java.util.Map">
        SELECT
        c.*,a.name
        FROM circle c LEFT JOIN account a ON c.account_id=a.id
        ORDER BY c.create_time DESC
        <if test="beginNum !=null">
            limit #{beginNum},#{limitSize}
        </if>
    </select>

    <select id="getCircleSearchList" parameterType="java.util.Map" resultType="java.util.Map">
        SELECT
        c.*,a.name
        FROM circle c LEFT JOIN account a ON c.account_id=a.id
        ORDER BY c.create_time DESC
        <if test="beginNum !=null">
            limit #{beginNum},#{limitSize}
        </if>
    </select>

    <select id="getCircleByIdList" parameterType="java.util.Map" resultType="java.util.Map">
        SELECT
        c.*
        FROM circle c
        <where>
            c.account_id = #{account_id}
        </where>
        ORDER BY c.create_time DESC
        <if test="beginNum !=null">
            limit #{beginNum},#{limitSize}
        </if>
    </select>
</mapper>