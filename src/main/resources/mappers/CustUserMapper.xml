<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.facerun.dao.CustUserMapper">

    <select id="getUsers" resultType="com.facerun.bean.Record">
        SELECT
        a.id,a.account,a.name,a.phone,a.status,a.type,a.create_at AS createAt,a.head_portrait AS headPortrait
        FROM account a
    </select>
</mapper>